version: '3'

services:
  strapi:
    container_name: garn-monitoring
    image:  hayd/alpine-deno:1.5.4
    environment:
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT}
      SMTP_USERNAME: ${SMTP_USERNAME}
      SMTP_PASSWORD: ${SMTP_PASSWORD}
      SMTP_FROM: ${SMTP_FROM}
      SMTP_TO: ${SMTP_TO}
      LOG_LEVEL: ${LOG_LEVEL}
    working_dir: /app
    command: deno run -A --unstable app.ts
    # command: deno run -A --unstable app.ts --once
    volumes:
      - .:/app
