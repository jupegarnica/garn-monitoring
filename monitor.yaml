log_level: NOTSET
run_every: 60000
request_timeout: 10000
requests:
  - jupegarnica.com
  - ups
smtp:
  host: '${{SMTP_HOST}}'
  username: '${{SMTP_USERNAME}}'
  password: '${{SMTP_PASSWORD}}'
  port: '${{SMTP_PORT}}'
  from: '${{SMTP_FROM}}'
  to: '${{SMTP_TO}}'
